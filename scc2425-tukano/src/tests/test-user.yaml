config:
  target: https://tukano-1730388952740.azurewebsites.net/tukano/rest
  phases:
    - name: simple_post
      duration: 1
      arrivalRate: 1

scenarios:
  - name: TuKanoRegister
    flow:
      # 1. Criar User (/users/{userId}) DONE
      - post:
          url: /users/
          headers:
            Content-Type: application/json
          json:
            userId: "TesteCriar" # this parameter is filled from the fields above
            pwd: "password"
            email: "email "
            displayName: "displayName"

      # 2. Obter User (GET /users/{userId}) DONE
      - get:
          url: /users/TesteCriar?pwd=password
          headers:
            Content-Type: application/json

      # 3. Atualizar User (PUT /users/{userId}) DONE
      - put:
          url: /users/TesteCriar?pwd=password
          headers:
            Content-Type: application/json
          json:
            userId: "TesteCriar"
            pwd: "newpassword"
            email: "UpdatedUser@gmail.com"
            displayName: "NewDisplayName"

#      # 4. Buscar User (GET /users?query={pattern})
      - get:
          url: /users?query=Teste
          headers:
            Content-Type: application/json

      # 5. Excluir User (DELETE /users/{userId}) DONE
      - delete:
          url: /users/TesteCriar?pwd=password
          headers:
            Content-Type: application/json


